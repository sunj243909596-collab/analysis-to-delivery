# 开发设计说明书：{{ cookiecutter.project_name }}

> 版本：{{ cookiecutter.version }}
> 日期：{{ cookiecutter.date }}
> 状态：⬜ 草稿 / ✅ 已确认
> 对应阶段：阶段 8（开发设计，代码实现版）

## 一、架构设计

### 1.1 技术栈
（参考 config/tech-stack/）

### 1.2 分层架构

```
Controller/ → Service/ → Repository/ → Entity/
```

### 1.3 关键依赖

| 依赖 | 版本 | 用途 |
|---|---|---|

## 二、模块设计

### 2.1 {模块名}

#### 后端

##### Controller

```java
@RestController
@RequestMapping("/api/v1/{module}")
public class {Module}Controller {
    // ...
}
```

##### Service

```java
@Service
public class {Module}Service {
    // ...
}
```

##### Repository

```java
public interface {Module}Repository extends BaseMapper<{Module}Entity> {
    // ...
}
```

##### Entity

```java
@Data
@TableName("{table_name}")
public class {Module}Entity {
    // 字段
}
```

##### Mapper XML

```xml
<mapper namespace="...{Module}Repository">
    <select id="..." resultType="...">
        SELECT ...
    </select>
</mapper>
```

##### DTO/VO

```java
@Data
public class {Module}DTO {
    // 字段
}
```

##### 事务与异常

- 事务边界：
- 异常处理：

#### 前端（PC 端）

##### 组件

```vue
<template>
  <div>
    <!-- 模板 -->
  </div>
</template>

<script setup lang="ts">
// 逻辑
</script>
```

##### 路由

```typescript
{
  path: '/{module}',
  component: () => import('@/views/{module}/Index.vue'),
  meta: { title: '...' }
}
```

##### API

```typescript
export const get{Module}List = (params: any) =>
  request.get('/api/v1/{module}', { params });
```

#### 移动端 / APP（如适用）

```vue
<!-- uni-app 组件 -->
```

## 三、联调说明

### 3.1 接口清单

| 接口 | 方法 | 路径 | 入参 | 出参 | 错误码 |
|---|---|---|---|---|---|

### 3.2 联调步骤

1. 启动后端
2. 启动前端
3. 触发业务场景
4. 验证数据流
5. 验证业务记账 / 副作用

### 3.3 Mock 数据

```json
{
  "...": "..."
}
```

## 四、Checklist

- [ ] Controller 完整
- [ ] Service 完整
- [ ] Repository 完整
- [ ] Entity 完整
- [ ] Mapper XML 完整
- [ ] DTO/VO 完整
- [ ] 前端组件完整
- [ ] 移动端组件完整（如适用）
- [ ] 联调说明完整
- [ ] Mock 数据完整
- [ ] 错误码映射完整
- [ ] 事务边界明确
- [ ] 异常处理完整

## 五、设计回测（v1.3，HARD GATE）

> 参考 skill 内 `references/design-backtest.md`。
> 不通过禁止进入阶段 8。

### 5.1 数据模型回测

| 表 | 样本量 | 结果 | 备注 |
|---|---|---|---|
| | | ✅ / ⚠️ / ❌ | |

### 5.2 业务规则回测

| 场景 | 样本量 | 命中 | 异常 | 备注 |
|---|---|---|---|---|
| | | | | |

### 5.3 状态机回测

- 合法跃迁数：
- 历史回放样本：
- 非法跃迁：{N}（详情见附表）

### 5.4 字段对齐回测

- `field-alignment-check.py` 跑完：P0={N} P1={N} P2={N}
- 自创字段：{N}（清单）
- 废弃字段：{N}（清单）

### 5.5 回测结论

- [ ] ✅ 通过 → 进入阶段 8
- [ ] ⚠️ 带条件通过（条件：{...}）
- [ ] ❌ 不通过 → 回阶段 8 修复

签字：{name} {{ cookiecutter.date }}

## 六、子流程门控清单（v1.3-dev，可选实施扩展）

> 参考 skill 内 `references/stage-gate.md` 第 2 层 + 第 3 层。

| 子流程 | 状态 | 签字 / 时间 |
|---|---|---|
| brainstorming 设计稿 | ⬜ | |
| spec 自检 | ⬜ | |
| writing-plans 完成 | ⬜ | |
| TDD RED 测试 | ⬜ | |
| 全部子任务 GREEN | ⬜ | |

## 七、任务复盘汇总（v1.3）

> 每个 writing-plans 子任务完成后，按 skill 内 `references/task-retrospective.md` 的 5 问复盘写到 commit / PR。
> 此处汇总关键沉淀。

### 7.1 沉淀到知识库的通用知识

| 日期 | 知识条目 | 已沉淀到 |
|---|---|---|
| | | |

### 7.2 流程改进（下次项目用）

1. {...}
2. {...}

### 7.3 阶段 8 整体评估

- 子任务数：{N}（成功 {A} / 重做 {B}）
- 用时偏差：{百分比}
- 回测通过率：{百分比}
