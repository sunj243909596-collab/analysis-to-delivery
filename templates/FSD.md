# 功能规格说明书（FSD）：{项目名}

> 版本：v0.1
> 日期：{日期}
> 状态：⬜ 草稿 / ✅ 已确认
> 对应 V1（存储过程版 / 详细设计版）

## 一、模块清单

| 模块 | 优先级 | 备注 |
|---|---|---|

## 二、PL/SQL 包清单

| 包名 | 用途 | 关联表 |
|---|---|---|

## 三、包规范

### 3.1 {包名} 包规范

```sql
CREATE OR REPLACE PACKAGE {PKG_NAME} AS
  -- 类型定义
  -- 过程声明
  -- 函数声明
END {PKG_NAME};
```

## 四、包体实现

### 4.1 {包名} 包体

```sql
CREATE OR REPLACE PACKAGE BODY {PKG_NAME} AS
  -- 私有变量
  -- 过程实现
  -- 函数实现
END {PKG_NAME};
```

## 五、核心 SQL

### 5.1 {SQL 名称}

```sql
-- 业务说明
SELECT ...
FROM ...
WHERE ...;
```

## 六、错误码定义

| 错误码 | 含义 | 抛出位置 |
|---|---|---|

## 七、tXML 接口

（如适用）

## 八、部署清单

- [ ] 创建表
- [ ] 创建索引
- [ ] 创建序列
- [ ] 创建包
- [ ] 初始化数据
