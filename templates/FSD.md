# 功能规格说明书（FSD）：{项目名}

> 版本：v0.1
> 日期：{日期}
> 状态：⬜ 草稿 / ✅ 已确认
> 对应阶段：阶段 8（开发设计）

## 一、模块清单

| 模块 | 优先级 | 涉及表 / 接口 | 备注 |
|------|--------|--------------|------|
| | | | |

## 二、功能详述

### 2.1 {模块名}

#### 输入

| 项 | 类型 | 必填 | 来源 | 校验规则 |
|----|------|------|------|----------|
| | | | | |

#### 输出

| 项 | 类型 | 必有 | 说明 |
|----|------|------|------|
| | | | |

#### 业务规则

1. 规则 1
2. 规则 2
3. ...

#### 边界条件

| 场景 | 期望行为 |
|------|---------|
| 空输入 | |
| 极值（最大 / 最小） | |
| 重复提交 | |
| 并发冲突 | |

#### 异常路径

| 场景 | 错误码 | 处理方式 |
|------|--------|---------|
| 业务校验失败 | | 返回 + 提示 |
| 数据不存在 | | 返回 + 提示 |
| 权限不足 | | 返回 + 跳转登录 |
| 系统异常 | | 日志 + 告警 + 通用错误 |

## 三、接口契约

### 3.1 REST API

| 接口 | 方法 | 路径 | 入参 | 出参 | 错误码 |
|------|------|------|------|------|--------|
| {名称} | GET / POST / PUT / DELETE | /api/v1/{path} | （DTO） | （VO） | |

#### 3.1.1 {接口名}

**请求**：

```http
POST /api/v1/{path}
Content-Type: application/json

{
  "field1": "value1",
  "field2": "value2"
}
```

**响应（成功）**：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

**响应（失败）**：

```json
{
  "code": 1001,
  "message": "参数错误：xxx"
}
```

### 3.2 RPC / 消息（如适用）

（Dubbo / MQ 事件等）

## 四、状态机

### 4.1 {业务对象名} 状态流转

```
初始态
   ↓
中间态 A ─→ 中间态 B ─→ 终态
   ↓           ↓
  异常态     异常态
```

**状态枚举**：

| 状态码 | 状态名 | 进入条件 | 退出条件 |
|--------|--------|----------|----------|
| | | | |

**禁止流转**（如：终态不可回退到中间态）

## 五、数据模型概览

### 5.1 涉及的表

| 表名 | 用途 | 读写 |
|------|------|------|
| | | | 

### 5.2 关键字段

| 字段 | 类型 | 含义 | 来源（库.表.字段） |
|------|------|------|-------------------|
| | | | |

> 详细 DDL 见 `03-数据模型设计.md`

## 六、错误码字典

### 6.1 业务错误码

| 错误码 | 含义 | 抛出位置 | HTTP 状态 |
|--------|------|----------|-----------|
| | | | |

### 6.2 系统错误码（如适用）

（如有框架级 / 平台级错误码映射）

## 七、依赖与约束

### 7.1 外部依赖

| 系统 / 服务 | 用途 | 接口 | 可用性假设 |
|------------|------|------|-----------|
| | | | |

### 7.2 业务约束

- 合规条款：（详见 `04-合规评审.md`）
- 业务规则：必须 X / 禁止 Y

## 八、变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| v0.1 | | 初稿 | |
