# 数据模型设计：{项目名}

> 版本：v0.1
> 日期：{日期}
> 状态：⬜ 草稿 / ✅ 已确认

## 一、模型概览

（用 ASCII 或 Mermaid 画 ER 图）

## 二、表清单

| 表名 | 用途 | 新增/扩展 | 备注 |
|---|---|---|---|
| | | | |

## 三、表设计

### 3.1 {表名}

**用途**：
**新增/扩展**：
**关联**：

```sql
CREATE TABLE {TABLE_NAME} (
    ID NUMBER PRIMARY KEY,
    -- 业务字段
    -- 审计字段（5 件套）
    CREATE_DATE_TIME DATE DEFAULT SYSDATE NOT NULL,
    CREATE_USER_ID   VARCHAR2(50) NOT NULL,
    MOD_DATE_TIME    DATE DEFAULT SYSDATE NOT NULL,
    MOD_USER_ID      VARCHAR2(50) NOT NULL,
    WM_VERSION_ID    NUMBER DEFAULT 1 NOT NULL
);

COMMENT ON TABLE {TABLE_NAME} IS '{表注释}';
COMMENT ON COLUMN {TABLE_NAME}.ID IS '主键';

-- 索引
CREATE INDEX IDX_{TABLE_NAME}_{COL} ON {TABLE_NAME}({COL});

-- 序列
CREATE SEQUENCE SEQ_{TABLE_NAME} START WITH 1 INCREMENT BY 1;
```

## 四、字段对齐验证

详见 `REVIEW_字段对齐分析.md`

## 五、初始化数据

```sql
INSERT INTO {TABLE_NAME} (...) VALUES (...);
```

## 六、迁移脚本（如适用）

```sql
-- V1.0 → V1.1
ALTER TABLE {TABLE_NAME} ADD (...);
```
